
<div class="container">
  <h1>List Index</h1>

    <% @lists.each do |list| %>
      <li><%=link_to list.name, list_path(list)%></li>
    <% end %>
    <br>
    <%=link_to "Create list", new_list_path,class:"btn btn-primary" %>
</div>
